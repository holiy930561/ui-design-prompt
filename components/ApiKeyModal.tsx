import React from 'react';

// This component is deprecated as API keys must be managed via process.env.
// Content removed to prevent compilation errors due to missing UI constants.
export const ApiKeyModal: React.FC<any> = () => {
  return null;
};